#include <stdio.h>
#include <unistd.h>


typedef struct arquivoStruct* Sys(FILE* file){
    struct arquivoStruct* arq;
  //
   
    return arquivoStruct;

} Sys ;

int main(){
    FILE* novo;
  
    struct arquivoStruct* file;
    novo = fopen("arquivo.dsc", "wb");

    file = Sys(arquivo);

    return 0;
};